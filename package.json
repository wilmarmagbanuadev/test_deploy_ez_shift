{
  "name": "web",
  "version": "0.1.0",
  "private": true,
  
  "scripts": {
    "start": "npm install -g serve",
    "build": " serve -s build",
    "test": "react-scripts test",
    "eject": "react-scripts eject",
    "postinstall": "patch-package"
  }
 
}
